1 часть
select round(avg(experience)) as avgExp, repair_firms.name from masters
join repair_firms on firm_id = repair_firms.id
group by repair_firms.name
order by avgExp desc

2 часть
select round(avg(experience)) as avgExp from masters
